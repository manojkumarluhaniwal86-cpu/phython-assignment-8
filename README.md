# phython-assignment-8
A simple Flask web application that handles user registration using HTML forms. It collects user details (Name, City, Phone Number), processes form submission using Flask routes, and displays a confirmation page with submitted data.
